# ==============================================================
# Root Portfolio — Variáveis de Ambiente / Environment Variables
# Copie este arquivo para `.env` e preencha os valores.
# Copy this file to `.env` and fill in the values.
# NUNCA versione o arquivo `.env` real. / NEVER commit your real `.env`.
# ==============================================================

# Webhook do Discord que recebe as mensagens do formulário de contato.
# Discord webhook URL that receives the contact form messages.
# Crie em: Configurações do Servidor > Integrações > Webhooks
VITE_DISCORD_WEBHOOK_URL=

# --- Opcionais (não usados pelo código atual, reservados para extensões) ---
# VITE_GOOGLE_ANALYTICS_ID=
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
